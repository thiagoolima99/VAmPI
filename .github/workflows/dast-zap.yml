name: DAST - OWASP ZAP

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  dast-zap:
    name: DAST - OWASP ZAP (VAmPI)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Start VAmPI
        run: |
          docker compose up -d
          echo "Aguardando VAmPI iniciar..."
          sleep 30

      - name: Verify VAmPI is running
        run: |
          curl -v http://localhost:5002 || true
          curl -v http://localhost:5002/openapi.json || true

      - name: ZAP API Scan
        uses: zaproxy/action-api-scan@v0.9.0
        with:
          target: 'http://localhost:5002/openapi.json'
          allow_issue_writing: false
          fail_action: false
        continue-on-error: true

      - name: Upload ZAP report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: zap-report-vampi
          path: report_html.html
          retention-days: 30

      - name: Stop VAmPI
        if: always()
        run: docker compose down
